// Temporary placeholder crate so dependent builds can proceed.
// Replace with real SBE code when available.
#![allow(dead_code)]

pub mod wire {
    // minimal placeholders you can expand later
    pub struct TradeEvent;
    pub struct BookTicker;
}